esphome:
  name: sonoff_basic
  platform: ESP8266
  board: esp01_1m

wifi:
  ssid: "Keenetic-0737-2.4"
  password: "SGEQfzJ7"
#  power_save_mode: none
  
# Optional manual IP
  manual_ip:
    static_ip: 192.168.1.118
    gateway: 192.168.1.1
    subnet: 255.255.255.0  
  
  # Enable fallback hotspot (captive portal) in case wifi connection fails
#  ap:
#    ssid: "Sonoff Basic Fallback Hotspot"
#    password: "W4GaHyfSfSMV"

#captive_portal:

# Enable logging
logger:

# Enable Home Assistant API
api:
  password: c2h5oh
ota:

binary_sensor:
  - platform: gpio
    pin:
      number: GPIO0
      mode: INPUT_PULLUP
      inverted: True
    name: "Sonoff Basic Button"
    on_press:
      - switch.toggle: relay

switch:
  - platform: gpio
    name: "Sonoff Basic Relay"
    pin: GPIO12
    id: relay

status_led:
  pin:
    number: GPIO13
    inverted: yes